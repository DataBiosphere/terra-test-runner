pluginManagement {
    repositories {
        gradlePluginPortal()
    }
    plugins {
        id 'com.google.cloud.artifactregistry.gradle-plugin' version '2.2.5'
    }
}

rootProject.name = 'terra-test-runner'
